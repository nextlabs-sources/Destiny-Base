id 123 status draft creator empty access_policy empty resource big_composite =
    DESCRIPTION "blah blah blah"
    NAME != "foo.txt" and NAME = "bar.doc"
    OR  TYPE != "PERL" and TYPE == "JAVA"
    OR  DIRectory != "FOO" and directory = "BA\"R"
    OR  SIZE = 100 AND SIZE == 100 AND SIZE < 1000 AND SIZE > 10 AND SIZE <= 100 and SIZE >= 100 and custom != null
    OR  key1 = [1, 2, 3] AND key2 = [ "a", "b", "c\"\\d" ]
    OR  trUE and False
    
